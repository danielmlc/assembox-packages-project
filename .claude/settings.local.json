{
  "permissions": {
    "allow": [
      "Bash(pnpm install)",
      "Bash(pnpm install --filter assembox-builder --filter assembox-storage)",
      "Bash(pnpm install --ignore-workspace)",
      "Bash(xargs grep -l \"HasPrimaryFullEntity\")"
    ]
  }
}
